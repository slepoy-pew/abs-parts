import styled from "styled-components";
import imageRequest from "../../../assets/imageRequest.jpg";

const StyledRequestPage = styled.div`
    background-image: url(${imageRequest});
    object-fit: cover;
    height: 100vh;
`;

export default StyledRequestPage;
